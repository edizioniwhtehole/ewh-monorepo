# Copia questo file in `.env` e riempi i valori necessari.
# Esempio: cp .env.template .env

# --- Scalingo ---
# Token personale (obbligatorio per interrogare le API Scalingo)
SCALINGO_API_TOKEN=tk-us-DhQRzWtrmpF68tPH9nUcpicH1Zy6xz-pbi87O82akbDgSAUh
# Regione (facoltativo, default osc-fr1)
SCALINGO_REGION=osc-fr1
# Endpoint API custom (facoltativo)
# SCALINGO_API_URL=https://api.osc-fr1.scalingo.com

# --- Bucket S3 ---
# Elenco bucket separati da spazi o newline.
STATUS_BUCKETS=""
# In alternativa, indica un file con un bucket per riga (vedi bucket_placeholder.txt)
STATUS_BUCKETS_FILE=tools/dev-dashboard/config/buckets_placeholder.txt
STATUS_BUCKETS_ENDPOINT=https://s3.eu-west-2.wasabisys.com

# Credenziali Wasabi/AWS (necessarie per usare aws s3 ls)
# Copia i valori da infra/wasabi.env o dal tuo secreto manager.
AWS_ACCESS_KEY_ID=H2A9Y2KNAEUN1EMABX8D
AWS_SECRET_ACCESS_KEY=QtSMT5DzJ0PA1VzKqpY92YH5XOOAPR6koYJWsaDH
AWS_DEFAULT_REGION=eu-west-2
WASABI_ENDPOINT=s3.eu-west-2.wasabisys.com

# --- Database Postgres ---
# URL di connessione (puoi elencarne più di uno separati da spazi/newline)
STATUS_DB_URLS=""
# O un file con un URL per riga (vedi databases_placeholder.txt)
STATUS_DB_URLS_FILE=tools/dev-dashboard/config/databases_placeholder.txt

# --- Percorso config Scalingo (facoltativo, se vuoi usare il file locale) ---
#SCALINGO_CONFIG_FILE=/Users/<utente>/.config/scalingo/config.json
